# Weather App

A simple weather app that show current weather for city via searchbox. The project aims to familiarize the use of API in your projects.

## 🚀 Features

-   **WeatherAPI**: Get data from weather API.
-   **GiphyAPI**: Get GIF from Giphy and set it as background image.
-   **Cards**: shows current weather info of city 
-   **History**: show history of search in side bar

## 🛠 Technologies Used

-   **HTML5 & CSS3**
-   **JavaScript (ES6+)**
-   **Webpack** (Module Bundler)
-   **NPM** (Package Manager)

## 📂 Project Structure

The code is organized using the **MVC (Model-View-Controller)** pattern logic:

-   `src/index.js` (Controller): The entry point. Connects the Logic and  handles event listeners.
-   `src/cityCard.js` Update info in city Card with current weather info. 
-   `src/hourlyWeatherInfo.js` Shows hourly weather info in the footer.
-    `src/sideBar.js` Rendering recent searches
-   `dist/`: Contains the production-ready bundled files.



## 👤 Author

**Sherikov Nafasbek**

-   GitHub: [@Sherikov](https://github.com/Sherikov)
